version: 2
jobs:
  test:
    docker:
      - image: circleci/node:8
    steps:
      - checkout
      - run:
          name: Install
          command: yarn install
      - run:
          name: Test
          command: yarn test

workflows:
  version: 2
  test-flow:
    jobs:
      - test
